# Wifi-Password-Ejector
Wi-Fi Profile Utility The second program is a Windows-only command-line utility that retrieves saved Wi-Fi profile names using Windows netsh commands and attempts to display the stored Wi-Fi key for each profile.
